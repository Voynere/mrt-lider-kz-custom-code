# Сессия 2026-08-14 — прайс KZ, льготный UI, цифры на главной

## Контекст

Домен **mrt-lider.kz** (отдельный WP на `91.207.75.94`). Параллельно обновляли услуги городов Казахстана и на **mrt-lider.ru**.

Источник прайса: `ПРАЙС МРТ НОВЫЙ (1).docx` (Downloads).

## 1. Льготный прайс UI на KZ (как на RU)

Для `almaty` / `astana` / `karaganda` / `taldykorgan` (без animals):

- `si_price` — основная, `si_discount` — льготная
- Плашка «Условия льготной цены»: 10% льготным категориям; для остальных после 20:00 и 00:00
- Отображение: `базовая ₸` + `от … льготная`, без колонки «Скидка*»

Код: `inc/mrt-service-helpers.php`, `page-service-item.php`, стили в `style.css` / `style.min.css`.

Текст как на RU:

> Скидка 10% повторным, пенсионерам, инвалидам, многодетным, участникам ВОВ и близким родственникам.  
> Для остальных — после 20:00 и после 00:00 ежедневно по прайсу.

## 2. Прайс услуг: холестеатома + Дотагита

На **обоих** доменах (4 города KZ):

| Изменение | Детали |
|-----------|--------|
| Новое | «Головной мозг + холестеатома» — 40 500 / льготная 34 500 |
| Замена | Кларискан → Дотагита до 90 кг (16 000) и от 90 кг (32 000) |

CLI: `wp-content/plugins/services-importer/cli-update-kz-cholesteatoma-dotagita.php`  
Workflow: `.github/workflows/update-kz-pricelist-cholesteatoma-dotagita.yml` (`workflow_dispatch`, `dry_run`).

После апдейта: `mrt_bump_page_cache_version()` (на RU), `wp cache flush`, PageSpeed `cache.flush`.

Где смотреть:  
`/{city}/uslugi-i-ceny/price/mrt-1-5-tesla/` → блоки **«МРТ Головы»** и **«МРТ Дополнительные услуги»** (аккордеон).

## 3. Цифры на главной KZ

`home.php` about__numbers / about__why:

- **13** лет опыта (было 12)
- **58** филиалов (было 55)

## Коммиты (mrt-lider-kz-custom-code, main)

| SHA | Суть |
|-----|------|
| `4e6729b` | CLI + workflow прайса Dotagita/холестеатома |
| `976d82b` | Flush page/PageSpeed после апдейта |
| `bf09868` | Льготный UI как на RU |
| `bedf96c` | 13 лет / 58 филиалов |

RU-репозиторий: тот же CLI/workflow (`dad31e77`, `0a4bfdb5`) — данные в WP БД mrt-lider.ru.

## Проверено на проде

- https://mrt-lider.kz/almaty/uslugi-i-ceny/price/mrt-1-5-tesla/ — concessional + Дотагита + холестеатома
- https://mrt-lider.ru/almaty/uslugi-i-ceny/price/mrt-1-5-tesla/ — то же
- https://mrt-lider.kz/almaty/ — data-count 13 / 58

## Не сделано / backlog

- Полный синк всех цен из docx по городам KZ (сейчас только холестеатома + Дотагита; у Алматы/Талдыкоргана/Караганды остальные цены ещё старые относительно docx; Астана ближе к новому прайсу)
- Локального SSH к KZ/RU на этой машине нет — апдейты БД только через GitHub Actions + secrets
- Uncommitted на RU по другим городам (Владивосток/Иркутск/Уссурийск) — отдельный трек, не KZ

## Как повторить апдейт прайса

```bash
gh workflow run update-kz-pricelist-cholesteatoma-dotagita.yml \
  --repo Voynere/mrt-lider-kz-custom-code -f dry_run=false
gh workflow run update-kz-pricelist-cholesteatoma-dotagita.yml \
  --repo Voynere/mrt-lider -f dry_run=false
```
